module gTst();
   wire [31:0] out;
   reg [31:0]  in;
   reg [3:0]   flag;
   
   g
     A(.out(out), .in(in), .iflag(flag));

   initial
     begin
	flag = 'D2;
	in = 'H8115a738;
	#100 $display("%h", out);
     end
endmodule // gTst
